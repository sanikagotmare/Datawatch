# API Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Database — SQLite works out of the box
# For PostgreSQL later: pip install psycopg2-binary
sqlalchemy==2.0.30

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Data Science
pandas==2.2.2
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.4.2
matplotlib==3.9.0
seaborn==0.13.2

# AI / LLM — versions compatible with Python 3.11
google-generativeai==0.7.2
langchain==0.2.1
langchain-google-genai==1.0.7

# Vector DB — telemetry warnings are harmless
chromadb==0.5.0

# Utilities
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.3.0
httpx==0.27.0
aiofiles==23.2.1
apscheduler==3.10.4
pillow==10.3.0

# Feature 1: DB Connectors (MySQL driver — optional, only needed for MySQL)
# pip install pymysql  (install separately if using MySQL)

# Feature 2+3: Self-Healing (already included via pandas + scipy above)
# No new packages needed — uses existing pandas, scipy, sqlalchemy
